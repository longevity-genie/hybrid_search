name: hybrid_search
channels:
  - conda-forge
  - bioconda
  - pytorch
  - plotly
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - pytorch
  - pip:
      - langchain
      - pycomfort>=0.0.13
      - unstructured
      - sentence_transformers
      - opensearch-py
